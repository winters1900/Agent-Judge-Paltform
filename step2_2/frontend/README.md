# Frontend Scaffold

## 目标
提供任务2评估平台的前端骨架，用于实现任务列表、任务创建、运行监控、结果展示与对比分析等页面。

## 建议结构
- `src/pages/`：页面视图
- `src/components/`：通用组件
- `src/api/`：接口调用封装
- `src/hooks/`：可复用逻辑
- `src/styles/`：样式资源

## 技术约定
- 使用 React + TypeScript 构建页面
- 使用 Ant Design 构建管理界面
- 使用 ECharts 展示统计图表
- 使用 WebSocket 接收实时状态更新
