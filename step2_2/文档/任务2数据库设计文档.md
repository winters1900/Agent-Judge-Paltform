# 任务2 数据库设计文档

## 1. 文档概述

### 1.1 文档名称
通用 Agent 评估平台数据库设计文档

### 1.2 设计目标
本文档用于定义通用 Agent 评估平台的数据库结构，支撑评测目标管理、评测任务管理、评测执行、数据集管理、指标配置、过程轨迹记录、结果分析与报告导出等功能，并与当前 API 设计文档保持一致。平台面向不同类型、不同接入方式、不同能力侧重的 Agent 评估，同时可用示例场景展示完整流程。评测目标是统一抽象，既可以是 HTTP Agent、CLI Agent、Workflow Agent，也可以是其它可适配的被测系统。

### 1.3 设计原则
- 结构清晰，满足前后端分离和异步评测场景
- 评测目标、任务元数据、运行数据、样本数据、指标数据分表存储
- 过程轨迹与结果汇总分离，便于查询优化
- 支持扩展自定义指标和多种评估方法
- 关键字段建立索引，保证列表查询和详情查询性能
- 兼顾通用 Agent 评估与示例场景展示
- 通过统一抽象支持不同类型的被测对象和接入协议

---

## 2. 数据库选型

### 2.1 主数据库
- **MySQL 8.x**

### 2.2 选型理由
- 支持事务，适合任务状态流转与结果持久化
- JSON 字段可存储灵活配置
- 索引与查询能力成熟，适合管理型平台
- 与主流后端框架兼容性好，部署简单

### 2.3 辅助存储建议
- **Redis**：用于任务队列、运行状态缓存、实时进度缓存
- **对象存储/文件系统**：用于评测报告导出文件、数据集文件、日志文件

---

## 3. 数据表设计

## 3.1 评测目标表 `evaluation_target`
用于记录被测 Agent 或被测系统的统一接入信息。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| target_code | varchar(64) | UNIQUE, NOT NULL | 评测目标编码 |
| target_type | varchar(64) | NOT NULL | 目标类型，如 HTTP Agent、CLI Agent、Workflow Agent |
| name | varchar(128) | NOT NULL | 目标名称 |
| description | text | NULL | 目标描述 |
| version | varchar(64) | NOT NULL | 目标版本 |
| endpoint | varchar(255) | NULL | 接入地址 |
| adapter_type | varchar(64) | NOT NULL | 适配器类型 |
| adapter_config | json | NOT NULL | 适配器配置 |
| input_schema | json | NULL | 输入结构定义 |
| output_schema | json | NULL | 输出结构定义 |
| enabled | tinyint(1) | NOT NULL | 是否启用 |
| created_at | datetime | NOT NULL | 创建时间 |
| updated_at | datetime | NOT NULL | 更新时间 |

### 索引建议
- `idx_target_type(target_type)`
- `idx_target_enabled(enabled)`
- `idx_target_created_at(created_at)`

---

## 3.2 评测任务表 `evaluation_task`
用于记录评测任务的基本信息、配置和状态。

#### 任务状态统一约定
- `draft`：草稿
- `pending`：待执行
- `running`：执行中
- `succeeded`：成功
- `failed`：失败
- `cancelled`：已取消
- `archived`：已归档

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| task_code | varchar(64) | UNIQUE, NOT NULL | 任务编码 |
| name | varchar(128) | NOT NULL | 任务名称 |
| description | text | NULL | 任务描述 |
| target_id | bigint | NOT NULL | 关联评测目标 |
| target_type | varchar(64) | NOT NULL | 目标类型快照 |
| target_version | varchar(64) | NOT NULL | 目标版本快照 |
| dataset_id | bigint | NOT NULL | 数据集 ID |
| status | varchar(32) | NOT NULL | 任务状态 |
| metric_config | json | NOT NULL | 指标配置 |
| evaluation_method_config | json | NOT NULL | 评估方法配置 |
| run_config | json | NOT NULL | 运行配置 |
| input_schema | json | NULL | 任务级输入结构 |
| output_schema | json | NULL | 任务级输出结构 |
| created_by | bigint | NULL | 创建人 |
| updated_by | bigint | NULL | 更新人 |
| created_at | datetime | NOT NULL | 创建时间 |
| updated_at | datetime | NOT NULL | 更新时间 |
| deleted_at | datetime | NULL | 软删除时间 |

### 索引建议
- `idx_task_status(status)`
- `idx_task_target_id(target_id)`
- `idx_task_dataset_id(dataset_id)`
- `idx_task_created_at(created_at)`

---

## 3.3 评测运行表 `evaluation_run`
用于记录某个评测任务的每次运行实例。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| run_code | varchar(64) | UNIQUE, NOT NULL | 运行编码 |
| task_id | bigint | FK, NOT NULL | 关联评测任务 |
| status | varchar(32) | NOT NULL | 运行状态 |
| progress | decimal(5,2) | NOT NULL | 运行进度 |
| current_sample_id | bigint | NULL | 当前处理样本 |
| retry_count | int | NOT NULL | 重试次数 |
| started_at | datetime | NULL | 开始时间 |
| ended_at | datetime | NULL | 结束时间 |
| summary | text | NULL | 运行摘要 |
| trace_id | varchar(128) | UNIQUE | 链路追踪 ID |
| error_message | text | NULL | 错误信息 |
| created_at | datetime | NOT NULL | 创建时间 |
| updated_at | datetime | NOT NULL | 更新时间 |

### 索引建议
- `idx_run_task_id(task_id)`
- `idx_run_status(status)`
- `idx_run_started_at(started_at)`
- `idx_run_current_sample_id(current_sample_id)`

---

## 3.4 数据集表 `dataset`
用于存储评测数据集的元信息。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| dataset_code | varchar(64) | UNIQUE, NOT NULL | 数据集编码 |
| name | varchar(128) | NOT NULL | 数据集名称 |
| description | text | NULL | 数据集描述 |
| source_type | varchar(32) | NOT NULL | 来源类型（手工/导入/API） |
| sample_count | int | NOT NULL | 样本数量 |
| version | varchar(64) | NOT NULL | 数据集版本 |
| status | varchar(32) | NOT NULL | 数据集状态 |
| created_by | bigint | NULL | 创建人 |
| created_at | datetime | NOT NULL | 创建时间 |
| updated_at | datetime | NOT NULL | 更新时间 |
| deleted_at | datetime | NULL | 软删除时间 |

### 索引建议
- `idx_dataset_status(status)`
- `idx_dataset_name(name)`
- `idx_dataset_version(version)`

---

## 3.5 数据集样本表 `dataset_sample`
用于存储数据集中的具体评测样本。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| dataset_id | bigint | FK, NOT NULL | 所属数据集 |
| sample_code | varchar(64) | UNIQUE, NOT NULL | 样本编码 |
| sample_type | varchar(32) | NOT NULL | 样本类型 |
| input_payload | json | NOT NULL | 输入内容 |
| expected_output | json | NULL | 期望输出 |
| reference_context | json | NULL | 参考上下文 |
| ground_truth | json | NULL | 标准答案或标签 |
| metadata | json | NULL | 扩展信息 |
| created_at | datetime | NOT NULL | 创建时间 |
| updated_at | datetime | NOT NULL | 更新时间 |

### 索引建议
- `idx_sample_dataset_id(dataset_id)`
- `idx_sample_type(sample_type)`

### 样本类型建议
- `generic_qa`
- `tool_use`
- `workflow`
- `multi_turn`
- `structured_output`
- `planning`
- `task_decomposition`
- `project_scaffold`
- `code_edit`
- `bug_fix`
- `command_execution`
- `snapshot_restore`
- `multi_turn_revision`

---

## 3.6 评估方法表 `evaluation_method`
用于记录评估方法定义，例如面向结果、面向过程、显式指标、模糊指标等。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| method_code | varchar(64) | UNIQUE, NOT NULL | 方法编码 |
| name | varchar(128) | NOT NULL | 方法名称 |
| category | varchar(32) | NOT NULL | 方法类别 |
| description | text | NULL | 方法说明 |
| config_schema | json | NULL | 配置结构定义 |
| enabled | tinyint(1) | NOT NULL | 是否启用 |
| created_at | datetime | NOT NULL | 创建时间 |
| updated_at | datetime | NOT NULL | 更新时间 |

### 索引建议
- `idx_method_category(category)`
- `idx_method_enabled(enabled)`

### 方法分类建议
- `result`
- `process`
- `explicit`
- `fuzzy`
- `custom`
- `ragas`
- `judge`
- `plugin`

---

## 3.7 指标定义表 `metric_definition`
用于存储系统内置指标和自定义指标定义。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| metric_code | varchar(64) | UNIQUE, NOT NULL | 指标编码 |
| name | varchar(128) | NOT NULL | 指标名称 |
| metric_type | varchar(32) | NOT NULL | 指标类型（显式/模糊/自定义） |
| dimension | varchar(32) | NOT NULL | 评估维度（效果/安全/性能） |
| description | text | NULL | 指标说明 |
| calc_mode | varchar(32) | NOT NULL | 计算方式（Ragas/规则/LLM-as-a-Judge/自定义） |
| config_schema | json | NULL | 指标配置结构 |
| enabled | tinyint(1) | NOT NULL | 是否启用 |
| created_at | datetime | NOT NULL | 创建时间 |
| updated_at | datetime | NOT NULL | 更新时间 |

### 索引建议
- `idx_metric_type(metric_type)`
- `idx_metric_dimension(dimension)`
- `idx_metric_enabled(enabled)`

### 指标类型建议
- `explicit`
- `fuzzy`
- `custom`

### 评估维度建议
- `effect`
- `safety`
- `performance`

### 指标示例建议
- `task_success_rate`
- `response_time`
- `token_usage`
- `tool_call_accuracy`
- `reasoning_quality`
- `interaction_quality`
- `command_success_rate`
- `build_success_rate`
- `code_edit_quality`
- `rollback_success_rate`
- `safety_violation_rate`

---

## 3.8 运行样本结果表 `evaluation_sample_result`
用于存储每个样本在评测运行中的详细结果。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| run_id | bigint | FK, NOT NULL | 运行 ID |
| sample_id | bigint | FK, NOT NULL | 样本 ID |
| status | varchar(32) | NOT NULL | 样本运行状态 |
| input_snapshot | json | NOT NULL | 输入快照 |
| output_snapshot | json | NULL | 输出快照 |
| score_summary | json | NULL | 样本评分汇总 |
| error_message | text | NULL | 错误信息 |
| started_at | datetime | NULL | 开始时间 |
| ended_at | datetime | NULL | 结束时间 |
| created_at | datetime | NOT NULL | 创建时间 |
| updated_at | datetime | NOT NULL | 更新时间 |

### 索引建议
- `idx_sample_result_run_id(run_id)`
- `idx_sample_result_sample_id(sample_id)`
- `idx_sample_result_status(status)`

---

## 3.9 评测过程轨迹表 `evaluation_trace`
用于记录 Agent 运行过程中的思考、决策、行动和观察信息。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| run_id | bigint | FK, NOT NULL | 运行 ID |
| sample_id | bigint | FK, NULL | 样本 ID |
| step_index | int | NOT NULL | 步骤序号 |
| phase | varchar(32) | NOT NULL | 阶段（plan/act/observe/reflect/replan/deliver） |
| decision | text | NULL | 决策内容 |
| observation | text | NULL | 观察内容 |
| state_snapshot | json | NULL | 状态快照 |
| tool_calls | json | NULL | 当前步骤工具调用列表 |
| created_at | datetime | NOT NULL | 创建时间 |

### 索引建议
- `idx_trace_run_id(run_id)`
- `idx_trace_sample_id(sample_id)`
- `idx_trace_step_index(step_index)`

---

## 3.10 工具调用日志表 `tool_call_log`
用于记录评测过程中 Agent 调用外部工具的详细信息。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| run_id | bigint | FK, NOT NULL | 运行 ID |
| sample_id | bigint | FK, NULL | 样本 ID |
| tool_name | varchar(128) | NOT NULL | 工具名称 |
| input_payload | json | NOT NULL | 输入参数 |
| output_payload | json | NULL | 输出参数 |
| success | tinyint(1) | NOT NULL | 是否成功 |
| error_type | varchar(64) | NULL | 错误类型 |
| duration_ms | int | NOT NULL | 耗时 |
| created_at | datetime | NOT NULL | 创建时间 |

### 索引建议
- `idx_tool_log_run_id(run_id)`
- `idx_tool_log_tool_name(tool_name)`
- `idx_tool_log_success(success)`

---

## 3.11 指标结果表 `metric_result`
用于存储运行级别或样本级别的指标计算结果。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| run_id | bigint | FK, NOT NULL | 运行 ID |
| sample_id | bigint | FK, NULL | 样本 ID，空表示运行级结果 |
| metric_id | bigint | FK, NOT NULL | 指标定义 ID |
| metric_value | decimal(18,6) | NULL | 指标值 |
| metric_text | text | NULL | 文本型指标结果 |
| metric_detail | json | NULL | 详细结果 |
| created_at | datetime | NOT NULL | 创建时间 |

### 索引建议
- `idx_metric_result_run_id(run_id)`
- `idx_metric_result_sample_id(sample_id)`
- `idx_metric_result_metric_id(metric_id)`

---

## 3.12 评测报告表 `evaluation_report`
用于存储评测报告汇总信息和导出文件路径。

### 字段设计
| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | bigint | PK, AI | 主键 |
| run_id | bigint | FK, NOT NULL | 运行 ID |
| report_title | varchar(128) | NOT NULL | 报告标题 |
| report_summary | text | NULL | 报告摘要 |
| report_path | varchar(255) | NULL | 报告文件路径 |
| report_format | varchar(32) | NOT NULL | 报告格式 |
| created_at | datetime | NOT NULL | 创建时间 |

### 索引建议
- `idx_report_run_id(run_id)`
- `idx_report_format(report_format)`

---

## 4. 表关系说明

### 4.1 核心关系
- `evaluation_target` 1:N `evaluation_task`
- `evaluation_task` 1:N `evaluation_run`
- `dataset` 1:N `dataset_sample`
- `evaluation_run` 1:N `evaluation_sample_result`
- `evaluation_run` 1:N `evaluation_trace`
- `evaluation_run` 1:N `tool_call_log`
- `evaluation_run` 1:N `metric_result`
- `evaluation_run` 1:1/1:N `evaluation_report`
- `evaluation_method` 与 `evaluation_task` 通过配置关联
- `metric_definition` 与 `metric_result` 通过 `metric_id` 关联

### 4.2 逻辑说明
- 评测目标只保存被测对象与接入适配信息，不保存评测运行明细
- 任务只保存配置和状态，不保存大量运行明细
- 运行表保存一次完整评测过程
- 样本结果表用于支撑单样本分析和问题定位
- 轨迹表与工具日志表用于过程可视化和异常排查
- 指标结果表用于统一汇总显示和对比分析

---

## 5. 数据字典建议

### 5.1 任务状态字典
- `draft`
- `pending`
- `running`
- `succeeded`
- `failed`
- `cancelled`
- `archived`

### 5.2 评估维度字典
- `effect`
- `safety`
- `performance`

### 5.3 指标类型字典
- `explicit`
- `fuzzy`
- `custom`

### 5.4 轨迹阶段字典
- `plan`
- `act`
- `observe`
- `reflect`
- `replan`
- `deliver`

### 5.5 评测目标类型字典
- `agent_http_api`
- `agent_cli`
- `workflow_agent`
- `retrieval_agent`
- `multi_turn_agent`
- `custom_adapter`

---

## 6. 性能与维护建议

### 6.1 分区或归档建议
- 运行明细较多时，可按月份或任务维度归档历史运行数据
- `evaluation_trace` 和 `tool_call_log` 数据量增长快，建议按运行时间分表或分区

### 6.2 缓存建议
- 任务列表、指标列表、方法列表、评测目标列表可缓存到 Redis
- 运行进度可实时写入缓存并通过 WebSocket 推送

### 6.3 数据清理建议
- 取消或失败任务保留原始日志，便于问题排查
- 报告文件和导出结果设置生命周期管理

---

## 7. 与 API 的对应关系

- `evaluation_target` 对应评测目标接口
- `evaluation_task` 对应任务管理接口
- `evaluation_run` 对应运行管理接口
- `dataset`、`dataset_sample` 对应数据集接口
- `evaluation_method`、`metric_definition` 对应方法与指标接口
- `evaluation_trace`、`tool_call_log` 对应过程查询接口
- `metric_result` 对应指标详情接口
- `evaluation_report` 对应报告导出接口
- `analysis_result` 对应多任务对比分析接口

---

## 8. 版本记录
- v1.0：初版数据库设计，覆盖任务、运行、数据集、样本、方法、指标、轨迹、日志、结果和报告
- v1.1：统一为“通用平台 + 示例场景”的表述，并调整样本类型和指标命名
- v1.2：补充评测目标表，增强对多类型 Agent 接入方式的支持
